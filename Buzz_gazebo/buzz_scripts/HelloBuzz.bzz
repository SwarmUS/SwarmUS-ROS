
BVMSTATE = "Hello"
counter=0
# Executed once at init time.
function init() {
}

# Executed at each time step.
function step() {
    counter = counter + 1
    log("Hello Buzz Beeeeeetches (counter =", counter, ")")
}

# Executed once when the robot (or the simulator) is reset.
function reset() {
    counter = 0
}

# Executed once at the end of experiment.
function destroy() {

}
